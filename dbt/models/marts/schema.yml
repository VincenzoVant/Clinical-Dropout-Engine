version: 2

models:
  - name: fct_patient_survival
    columns:
      - name: trial_id
        tests:
          - not_null
      - name: subjid
        tests:
          - not_null
      - name: event
        tests:
          - not_null
      - name: time_days
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - trial_id
            - subjid